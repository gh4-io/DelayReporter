<Window x:Class="DelayReporter.Views.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Delay Reporter"
        Height="330" Width="470"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{StaticResource WindowBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Delay Reporter" FontSize="20" FontWeight="SemiBold"
                   Foreground="{StaticResource AccentBrush}"/>

        <TextBlock Grid.Row="1" x:Name="VersionText" Margin="0,4,0,0"
                   Foreground="{StaticResource MutedTextBrush}"/>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock TextWrapping="Wrap"
                       Text="Reads a movement sheet, parses coded departure delays and writes a printable workbook."/>
            <TextBlock Style="{StaticResource SectionHeader}" Text="Mappings"/>
            <TextBlock x:Name="MappingsText" TextWrapping="Wrap" Style="{StaticResource Hint}"/>
            <TextBlock Style="{StaticResource SectionHeader}" Text="Settings"/>
            <TextBlock x:Name="SettingsText" TextWrapping="Wrap" Style="{StaticResource Hint}"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Close" Width="90" HorizontalAlignment="Right"
                IsDefault="True" IsCancel="True" Click="OnClose" Margin="0,12,0,0"/>
    </Grid>
</Window>
