<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Family palette, shared in spirit with ICS Scrubber and OFT Scrubber. -->
    <Color x:Key="AccentColor">#FF1F3864</Color>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="AccentLightBrush" Color="#FFD9E1F2"/>
    <SolidColorBrush x:Key="WindowBrush" Color="#FFF6F7F9"/>
    <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="BorderBrushSoft" Color="#FFD8DCE3"/>
    <SolidColorBrush x:Key="TextBrush" Color="#FF1B1B1B"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#FF5F6672"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#FFB45309"/>

    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Margin" Value="0,10,0,4"/>
    </Style>

    <Style x:Key="Hint" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
        <Setter Property="MinWidth" Value="90"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Padding" Value="4,3"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Margin" Value="0,2,10,2"/>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Margin" Value="0,2,10,2"/>
    </Style>

</ResourceDictionary>
